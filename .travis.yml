---

language: php
php:
  - 7.0
  - 7.1
  - 7.2
  - 7.3
  - 7.4
  - 8.0snapshot

stages:
  - analyze
  - test
  - release

jobs:
  fast_finish: true
  allow_failures:
    - php: 8.0snapshot
  include:
    - stage: analyze
      php: 7.4
      install:
        - phpenv config-rm xdebug.ini
        - composer install --prefer-dist
      script:
        - vendor/bin/phpunit
        - vendor/bin/php-cs-fixer --dry-run --stop-on-violation fix
        - composer validate

cache:
  directories:
    - $HOME/.composer
    - $HOME/.cache
    - build/cache

install:
  - phpenv config-rm xdebug.ini || true
  - composer remove --no-update --dev friendsofphp/php-cs-fixer ergebnis/composer-normalize
  - composer install --prefer-dist

script:
  - vendor/bin/phpunit

